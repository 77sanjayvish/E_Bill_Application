spring.application.name=E-Bill-Application
server.port=8080

# Database Configuration (MySQL Example)
spring.datasource.url=jdbc:mysql://localhost:3306/e_application
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Security Configuration
#jwt.secret=your_secret_key
#jwt.expiration=86400000  # Token expiry time in milliseconds (1 day)

# Spring Boot Logging Configuration
#logging.level.org.springframework=INFO
#logging.level.com.yourpackage=DEBUG

# CORS Configuration (If Needed)
#spring.web.cors.allowed-origins=http://localhost:3000
#spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
#spring.web.cors.allowed-headers=*

# Spring Boot Actuator (For Monitoring)
#management.endpoints.web.exposure.include=*

# Custom Application Properties
#app.name=Smart Meter Billing System
#app.version=1.0.0
